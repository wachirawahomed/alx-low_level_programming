#include "main.h"
#include <stdlip.h>
/**
 * strtow - splits a string into words.
 * @str: string to be split.
 * Return: pointer to an array of strings(words), NULL if str == NULL || ""
 * or if the function fails.
 */
char **strtow(char *str)
	char **s;

